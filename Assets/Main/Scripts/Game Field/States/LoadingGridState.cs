using UnityEngine;

namespace Main.Scripts.Game_Field.States
{
    public class LoadingGridState : BaseGridState
    {
        //State for loading data from last game exit

        public LoadingGridState(GridStateData data) : base(data)
        {
        }
    }
}
